# Link to Global Skill

This template uses the global `ship-it` skill for deployment.

To use: The skill is already available at the project level.

Quick deploy: Just say "ship it" and your app goes to `*.usenavi.app`
